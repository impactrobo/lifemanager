{
  "name": "lifemanager",
  "version": "1.0.0",
  "private": true,
  "description": "LIFEMan.EXE — single-page personal life-tracking web app (index.html + app.js).",
  "scripts": {
    "typecheck": "tsc -p tsconfig.json && tsc -p tsconfig.fx.json --noEmit",
    "build:fx": "tsc -p tsconfig.fx.json",
    "test": "node tests/run_all.js"
  },
  "devDependencies": {
    "playwright": "^1.49.0",
    "typescript": "^5.7.0"
  }
}
