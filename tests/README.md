# Tests

Canonical Playwright test scripts (`test_*.js`) go here — see `../CLAUDE.md` and
`../docs/ARCHITECTURE.md` for the list and conventions. This folder is currently empty;
recreating these as real, committed files is the first priority task for this repo.
